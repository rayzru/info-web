export async function seed() {
  console.log('+ apartments');
  // const data: (typeof apartments.$inferInsert)[] = [];
  // Array
  //   .from({ length: 100 }, (_, i) => i + 1)
  //   .forEach(n => data.push({ number: n }));

  // const result = await db.insert(apartments).values(data);

  // const map = new Map(result.map(r => [r.number, r.id]));

  // return { map };
}
